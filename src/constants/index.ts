export const API_ENDPOINT = String(process.env.REACT_APP_API_URL);

export const CAR_TYPE = {
  NEW: 'new',
  USED: 'used',
};

export const MILES = [100, 200, 300, 400, 500, 600, 700, 800, 900, 1000];

export const MIN_MILAGE = 0;
export const MAX_MILAGE = 100000;

export const DATE_TIME = 'MMMM D, YYYY | h:MM A';
